---
const { bgClass, id, orientation = 'flex-col-reverse' } = Astro.props
---

<section
  class={`${bgClass} flex min-h-screen lg:flex-row ${orientation} px-8 py-16   items-center justify-center  lg:py-8 lg:px-20`}
  id={id}>
  <slot />
</section>
